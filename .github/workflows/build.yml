name: "ZMK Build"

on:
  workflow_dispatch:
  pull_request:
  push:
    paths-ignore: ["README.md", ".github/workflows/draw.yaml"]

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@641514a97db345f499dd50b0360e594270f008fe # main
